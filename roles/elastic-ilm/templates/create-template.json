{
  "index_patterns": [
    "{{ item.key }}-*" 
  ],
  "settings": {
    "index": {
      "lifecycle": {
        "name": "{{ item.value }}", 
        "rollover_alias": "{{ item.key }}-alias" 
      }
    }
  }
}
